spring:
  application:
    name: config-server

  profiles:
    active: composite

  cloud:
    config:
      server:
        composite:
          - type: native
            search-locations: /Users/Dzmitry_Charapianka/Work/TR/Spring-cloud/config-server/src/main/resources/config
server:
  port: 8071