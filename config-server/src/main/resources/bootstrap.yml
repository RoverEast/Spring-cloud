spring:
  application:
    name: config-server

  profiles:
    active: composite

  cloud:
    config:
      server:
        composite:
          - type: native
            search-locations: /Users/dzmitrycharapianka/Work/TR/Spring Cloud/config-server/src/main/resources/config
server:
  port: 8071